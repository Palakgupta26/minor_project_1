<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zippify - File Compressor</title>
    <style>
        /* Reset some default styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            background-color: #121212; /* Dark background */
            background-image: linear-gradient(135deg, rgba(18, 18, 18, 0.8), rgba(0, 0, 0, 0.9)); /* Gradient effect */
            position: relative;
            overflow: hidden;
        }

        .container {
            background: rgba(255, 255, 255, 0.1); /* Slightly more opaque white */
            backdrop-filter: blur(10px); /* Increased blur for a more luxurious look */
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0px 10px 30px rgba(0, 0, 0, 0.5); /* Deeper shadow for a 3D effect */
            text-align: center;
            max-width: 600px;
            width: 100%;
        }

        h1 {
            color: #ffffff; /* Pure white for contrast */
            margin-bottom: 20px;
            font-size: 36px; /* Increased font size */
            font-weight: 700; /* Bold font */
            text-transform: uppercase; /* Uppercase letters */
            letter-spacing: 1px; /* Spacing for elegance */
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.7); /* Stronger shadow for depth */
        }

      
